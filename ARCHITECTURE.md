# 🏗️ System Architecture

## 📊 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         FRONTEND                             │
│                    (Nginx - Port 3000)                       │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Student    │  │    Admin     │  │   Landing    │     │
│  │   Portal     │  │    Panel     │  │     Page     │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                              │
│  HTML Pages (Bootstrap 5) + JavaScript (Fetch API)          │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ HTTP/REST API
                            │ (JWT Token)
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                         BACKEND                              │
│                  (Node.js + Express - Port 5000)             │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              API Routes + Middleware                  │  │
│  │  • JWT Verification                                   │  │
│  │  • CORS                                               │  │
│  │  • JSON Parser                                        │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Student    │  │    Admin     │  │  Complaint   │     │
│  │     API      │  │     API      │  │     API      │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                              │
│  Dependencies: bcrypt, jsonwebtoken, mongoose                │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ MongoDB Protocol
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                        DATABASE                              │
│                   (MongoDB - Port 27017)                     │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   students   │  │    admins    │  │  complaints  │     │
│  │  collection  │  │  collection  │  │  collection  │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                              │
│  Credentials: root/root                                      │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ (Optional)
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                     MONGO EXPRESS                            │
│                  (Web UI - Port 8081)                        │
│                                                              │
│  Database Management Interface                               │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Request Flow

### Student Login Flow
```
1. Student enters roll + password
   ↓
2. Frontend sends POST /student/login
   ↓
3. Backend validates credentials
   ↓
4. Backend generates JWT token
   ↓
5. Frontend stores token in localStorage
   ↓
6. Redirect to student dashboard
```

### Submit Complaint Flow
```
1. Student fills complaint form
   ↓
2. Frontend sends POST /complaints with JWT
   ↓
3. Backend verifies JWT token
   ↓
4. Backend saves to MongoDB
   ↓
5. Frontend shows success message
   ↓
6. Redirect to dashboard
```

### Admin Manage Complaint Flow
```
1. Admin changes status dropdown
   ↓
2. Frontend sends PUT /complaints/:id/status with JWT
   ↓
3. Backend verifies admin JWT
   ↓
4. Backend updates MongoDB
   ↓
5. Frontend reloads complaints + stats
```

## 🗂️ Data Flow

```
┌─────────────┐
│   Browser   │
│ (localStorage)│
│  - Token    │
│  - User Data│
└─────────────┘
      │
      │ JWT Token in Header
      ▼
┌─────────────┐
│   Backend   │
│  Middleware │
│  - Verify   │
│  - Decode   │
└─────────────┘
      │
      │ Mongoose Query
      ▼
┌─────────────┐
│   MongoDB   │
│ Collections │
│  - CRUD     │
└─────────────┘
```

## 🔐 Authentication Flow

```
┌──────────────────────────────────────────────────────────┐
│                    AUTHENTICATION                         │
└──────────────────────────────────────────────────────────┘

Signup:
  Password → bcrypt.hash(10) → Hashed Password → MongoDB

Login:
  Password → bcrypt.compare(hash) → Valid? → JWT Token
                                      ↓
                                   localStorage

Protected Route:
  Request → Extract Token → jwt.verify() → Valid? → Allow
                                            ↓
                                          Reject (401)
```

## 📦 Docker Container Network

```
┌─────────────────────────────────────────────────────────┐
│              hostel-network (Bridge)                     │
│                                                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐ │
│  │ frontend │  │ backend  │  │  mongo   │  │ mongo- │ │
│  │  :3000   │  │  :5000   │  │  :27017  │  │express │ │
│  │          │  │          │  │          │  │ :8081  │ │
│  └──────────┘  └──────────┘  └──────────┘  └────────┘ │
│       │             │              │            │       │
│       └─────────────┴──────────────┴────────────┘       │
│                                                          │
└─────────────────────────────────────────────────────────┘
         │              │              │            │
         │              │              │            │
    localhost:3000  localhost:5000  localhost:27017  localhost:8081
```

## 🎯 Component Responsibilities

### Frontend (Nginx)
- Serve static HTML/CSS/JS files
- Handle client-side routing
- Store JWT tokens
- Make API calls

### Backend (Node.js)
- Handle API requests
- Authenticate users
- Validate data
- Interact with database
- Generate JWT tokens

### Database (MongoDB)
- Store user data
- Store complaints
- Persist application state

### Mongo Express
- Provide web UI for database
- Debug and inspect data
- Manual data management

## 🔄 State Management

```
┌─────────────────────────────────────────────────────────┐
│                   CLIENT STATE                           │
│                                                          │
│  localStorage:                                           │
│    - studentToken: "jwt_token_here"                      │
│    - studentData: { roll, name, room, email }            │
│    - adminToken: "jwt_token_here"                        │
│    - adminData: { username }                             │
│                                                          │
│  Session:                                                │
│    - Current page                                        │
│    - Form data                                           │
└─────────────────────────────────────────────────────────┘
```

## 🛡️ Security Layers

```
Layer 1: Password Hashing (bcrypt)
         ↓
Layer 2: JWT Token Generation
         ↓
Layer 3: Token Verification Middleware
         ↓
Layer 4: Role-Based Access Control
         ↓
Layer 5: MongoDB Authentication
```

## 📊 Database Schema Relationships

```
┌─────────────┐
│  students   │
│  - roll (PK)│
│  - name     │
│  - password │
│  - room     │
│  - email    │
└─────────────┘
       │
       │ 1:N
       │
       ▼
┌─────────────┐
│ complaints  │
│  - _id (PK) │
│  - student_roll (FK)
│  - title    │
│  - description
│  - room     │
│  - status   │
│  - date     │
└─────────────┘

┌─────────────┐
│   admins    │
│  - username (PK)
│  - password │
└─────────────┘
```

## 🚀 Deployment Architecture

```
Development:
  Docker Compose → Local Containers → localhost

Production (Suggested):
  Frontend → Nginx/CDN
  Backend → Node.js Server (PM2)
  Database → MongoDB Atlas / Self-hosted
  Reverse Proxy → Nginx
  SSL → Let's Encrypt
```

## 📈 Scalability Considerations

```
Current: Single instance of each service
         ↓
Future:  Load Balancer
         ↓
    ┌────┴────┐
    │         │
Backend 1  Backend 2  (Horizontal scaling)
    │         │
    └────┬────┘
         ↓
    MongoDB Replica Set
```

---

**This architecture provides:**
- ✅ Separation of concerns
- ✅ Scalability
- ✅ Security
- ✅ Maintainability
- ✅ Easy deployment
